import { Meta } from '@storybook/addon-docs/blocks';
import './readmes.scss'

<Meta title="readmes/How To" />

# How To

<div className="readmes">

***

<div className="subheading">Run the Storybook App</div>

```sh
npm run storybook
```

Storybook will automatically launch to `http://localhost:6006`. It works with a watch process so saves are automatically
reflected in browser.

  <div className="tip-wrapper">
    <span className="tip">Tip</span>Destructive edits (adding, reorganizing, renaming) will require
    a restart.
  </div>
</div>